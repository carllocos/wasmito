name: Lint check

on:
  workflow_call:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      # - name: Set up Node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: 23

      # - name: Install Dependencies
      #   run: npm install

      # - name: Run Linter
      #   run: npm run lint
